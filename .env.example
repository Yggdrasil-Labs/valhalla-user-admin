# ===========================================
# 环境变量配置示例文件
# ===========================================
# 复制此文件为 .env 并根据需要修改配置值
# 注意：所有环境变量必须以 VITE_ 开头才能在客户端访问

# ===========================================
# 应用基础配置
# ===========================================

# 应用环境标识 (dev/test/prod)
# 用于区分不同部署环境
VITE_ENV=dev

# 应用名称
# 显示在页面标题和界面中
VITE_APP_NAME=Asgard Frontend

# ===========================================
# API 配置
# ===========================================

# API 基础地址
# 后端接口的基础 URL，不包含具体的接口路径
# 开发环境示例：http://localhost:8080/api
# 测试环境示例：https://test-api.yggdrasil-labs.com/api
# 生产环境示例：https://api.yggdrasil-labs.com/api
VITE_API_BASE_URL=http://localhost:8080/api

# ===========================================
# 环境说明
# ===========================================
# 
# 开发环境 (development):
# - VITE_ENV=dev
# - VITE_API_BASE_URL=http://localhost:8080/api
# 
# 测试环境 (test):
# - VITE_ENV=test
# - VITE_API_BASE_URL=https://test-api.yggdrasil-labs.com/api
# 
# 生产环境 (production):
# - VITE_ENV=prod
# - VITE_API_BASE_URL=https://api.yggdrasil-labs.com/api
# 
# ===========================================
# 注意事项
# ===========================================
# 
# 1. 所有环境变量必须以 VITE_ 开头
# 2. 修改环境变量后需要重启开发服务器
# 3. 生产环境部署时，确保设置正确的环境变量
# 4. 敏感信息（如 API 密钥）不应放在客户端环境变量中
# 5. 环境变量值不要包含引号，除非引号是值的一部分

# 测试模式标识
NODE_ENV=test

# 禁用一些开发工具以提高测试性能
VITE_DISABLE_DEVTOOLS=true

PLAYWRIGHT_BASE_URL=http://localhost:5173
TEST_USERNAME=demo
TEST_PASSWORD=password
STORAGE_STATE_PATH=storageState.json